# Used by Next rewrites to proxy /api/* to your backend.
# Example: https://your-render-service.onrender.com
BACKEND_URL=http://localhost:8000

# Optional: call the backend directly from the browser instead of rewrites.
# Example: https://your-render-service.onrender.com/api/v1
NEXT_PUBLIC_API_BASE=


